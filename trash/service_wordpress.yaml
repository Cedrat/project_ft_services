apiVersion: v1
kind: Service
metadata:
  name: service-wordpress
  annotations:
      metallb.universe.tf/allow-shared-ip: shared
spec:
  selector:
    app: wordpress
  type: LoadBalancer
  ports:
    - name: https
      protocol: TCP
      port: 5050
      targetPort: 5050
